server:
  port: 8080

spring:
  application:
    name: api-gateway
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/spring-boot-microservice-realm

  #Product Service route
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true