eureka:
  instance:
    hostname: localhost

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
    username: root
    password: Prabhu@12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: order-service

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql

server:
  port: 8081
